services:
  frontend:
    container_name: home-apps-dashboard
    image: home-apps/frontend
    build: .
    ports:
      - "3000:3000"
    working_dir: /app
    command: bash -c "npm run build && npm run start"
    restart: unless-stopped